close #300

# PR 描述：与业务系统打通数据（Issue #300）
## 一、背景与目标
当前 `ruoyi-ai` 可以进行通用对话，但尚未直接接入企业业务系统数据，导致类似“我今天有多少个任务”这类问题无法基于真实业务数据给出结果。

本 PR 目标是建立一套“业务系统 -> ruoyi-ai -> 对话回答”的标准化集成链路，使 AI 对话能够安全、可控、可观测地查询并返回业务系统实时数据。

## 二、需求问题（来自 Issue #300）
- 用户已有自有系统，希望把系统中的业务数据与 `ruoyi-ai` 打通；
- 聊天提问应可直接引用业务数据（如：今日任务数、待办数量、状态分布、逾期任务等）；
- 需要支持可扩展的数据接口，而不是一次性硬编码。

## 三、方案概览
### 1) 架构思路
采用“工具调用 + 业务 API 适配层”的模式：
1. 对话层识别用户查询意图（例如任务统计）；
2. 调用预定义业务工具（如 `getTaskStats`）；
3. 工具通过服务端适配器访问业务系统 API；
4. 返回结构化数据并生成自然语言回答。

### 2) 核心能力
- **数据连接能力**：可配置业务系统 Base URL、认证方式、超时与重试；
- **意图到接口映射**：将“任务数查询”等问题映射到对应业务接口；
- **统一响应格式**：标准化返回字段，保证前端展示与后续扩展；
- **多租户/多用户隔离**：按用户上下文传递身份与权限边界；
- **安全审计**：记录工具调用参数、响应摘要与错误信息（脱敏）。

## 四、主要改动设计
### 1) 新增业务系统适配层
- 增加 `BusinessDataClient`（或同类命名）：
  - 负责请求业务系统接口；
  - 统一处理认证、重试、异常、超时；
  - 对上层输出稳定 DTO。

### 2) 新增“任务统计”工具
- 增加 `getTaskStats` 工具（或 function call）：
  - 输入：用户 ID / 组织 ID / 日期范围（默认今天）；
  - 输出：总任务数、待处理、进行中、已完成、逾期等指标；
  - 支持“按天/按周”统计扩展。

### 3) 对话流程接入工具调用
- 在聊天服务中新增工具路由逻辑：
  - 识别任务类问题；
  - 触发 `getTaskStats`；
  - 将结构化结果拼装为可读回答（含关键数字和时间范围）。

### 4) 配置与开关
- 新增业务连接配置项（示例）：
  - `business.api.base-url`
  - `business.api.auth-type`
  - `business.api.token`
  - `business.api.connect-timeout-ms`
  - `business.api.read-timeout-ms`
- 增加功能开关：
  - `ai.tools.business-data.enabled=true/false`

### 5) 错误处理与降级
- 业务系统不可用时返回友好提示，不阻断会话；
- 区分“权限不足 / 接口失败 / 数据为空”三类错误；
- 记录错误码与 traceId，便于定位。

## 五、接口与交互示例
### 用户问题
- “我今天有多少个任务？”

### 工具调用（逻辑）
- `getTaskStats(userId, date=today)`

### 返回示例（结构化）
- `total=18, todo=6, doing=4, done=8, overdue=2`

### 最终回答示例
- “你今天共有 18 个任务，其中待处理 6 个、进行中 4 个、已完成 8 个，逾期 2 个。”

## 六、影响范围
- 聊天服务的工具调用链路；
- 业务系统 HTTP 访问层；
- 系统配置（新增业务连接与开关项）；
- 日志与监控（新增调用埋点）。

不影响：
- 既有非业务数据场景的普通对话能力；
- 不启用开关时，原流程保持不变。

## 七、安全与合规
- 所有外部请求必须携带最小权限凭证；
- 日志不落敏感字段（token、手机号、身份证号等）；
- 对用户可见回答仅展示其有权限访问的数据；
- 建议配合网关白名单和服务间 TLS。

## 八、测试与验收标准
### 1) 单元测试
- 工具输入校验与参数映射；
- 业务响应解析（正常/空数据/异常格式）；
- 错误码映射与降级文案。

### 2) 集成测试
- 联通真实/模拟业务系统：
  - 成功返回统计数据；
  - 超时与 5xx 重试；
  - 401/403 权限校验。

### 3) 验收用例（关键）
1. 用户提问“我今天有多少个任务”返回真实数据；
2. 不同用户查询结果隔离正确；
3. 业务系统不可用时返回可理解提示且系统稳定；
4. 关闭开关后功能无副作用回退。

## 九、发布与回滚
### 发布步骤
1. 配置业务系统连接参数；
2. 灰度开启 `ai.tools.business-data.enabled`；
3. 观察调用成功率、P95 延迟、错误率；
4. 全量开启。

### 回滚方案
- 关闭 `ai.tools.business-data.enabled` 即可快速回退至原有对话流程。

## 十、后续演进
- 支持更多业务对象：审批单、工单、客户、告警；
- 支持“统计 + 明细”多轮追问；
- 增加语义缓存，降低重复查询成本；
- 接入统一权限中心，细化字段级权限。

## TL;DR
本 PR 为 `ruoyi-ai` 增加“业务系统数据接入能力”，通过工具调用把“我今天有多少个任务”这类问题转为业务 API 查询并返回结构化结果，确保可扩展、可配置、可回滚、可审计，解决 Issue #300 的核心诉求。
